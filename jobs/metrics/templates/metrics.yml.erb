---
nats_servers:
<% p("nats.machines").each do |address| %>
- nats://<%= p("nats.user") %>:<%= p("nats.password") %>@<%= address %>:<%= p("nats.port") %>
<% end %>
name: <%= "#{spec.job.name}_#{spec.index.to_s}" %>
datadog_api_key: <%= p('datadog_api_key') %>
socket: <%= p('socket') %>
population: <%= p('population') %>
